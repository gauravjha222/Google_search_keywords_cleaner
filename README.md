# Google_search_keywords_cleaner
This Python script, filter_data.py, is an automated "cleaner" for keyword datasets. If you have a large spreadsheet of search queries (keywords) and you want to remove the "garbage" (misspellings, gibberish, non-English terms, or duplicate phrases) before feeding them into a machine learning model, this script does exactly that.
